{
    "version": 2,
    "buildCommand": "cd ../.. && pnpm install && pnpm turbo run build --filter=@bookly/api...",
    "installCommand": "cd ../.. && pnpm install",
    "outputDirectory": "dist",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/dist/index.js"
        }
    ]
}
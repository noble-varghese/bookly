{
    "version": 2,
    "buildCommand": "cd ../../ && pnpm install:ordered && pnpm turbo run build --filter=@bookly/api...",
    "installCommand": "pnpm install && pnpm install --dev",
    "outputDirectory": "dist",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/dist/index.js"
        }
    ]
}
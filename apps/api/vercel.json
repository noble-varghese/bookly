{
    "version": 2,
    "buildCommand": "cd ../.. && pnpm install && pnpm --dev && pnpm turbo run build --filter=@bookly/api...",
    "installCommand": "cd ../.. && pnpm install && pnpm --dev",
    "outputDirectory": "dist",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/dist/index.js"
        }
    ]
}
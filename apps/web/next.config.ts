import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['marketplace.canva.com', 'plus.unsplash.com', 'lh3.googleusercontent.com']
  },
};

export default nextConfig;
